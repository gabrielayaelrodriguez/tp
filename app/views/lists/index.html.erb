<div class="jumbotron text-center">
  <h1>Welcome to ToDoList app!</h1>
  <p>Most cutie page to keep your mind sharp</p> 
</div>

<div class="col-sm-12 text-center">
  <h3>Wanna make some ToDoLists?</h3>
  <p><%= link_to 'Make New List!', new_list_path, :class => "btn btn-success" %></p>
</div>


<h3>Last Lists Created by You</h3>
<% if defined?(@lists)  %>

<table class="table">
  <tr>
    <th>Name</th>
    <th>Url</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
 
  <% @lists.each do |list| %>
    <tr>
      <td><%= list.name %></td>
      <td><%= list.url %></td>
      <td><%= link_to 'Show', list_path(list) %></td>
      <td><%= link_to 'Edit', edit_list_path(list) %></td>
      <td><%= link_to 'Destroy', list_path(list),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
    </tr>
<% end %>
</table>

  
  <% else %>

  <h4>No lists yet, get started by adding some New List</h4>

  <% end %>


